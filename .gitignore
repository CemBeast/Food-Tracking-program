
Secrets.xcconfig

# Publishing/App Store prep — internal notes, marketing site, screenshots (not for the repo)
Publishing/

# Xcode per-user state (machine/user specific, should never be tracked)
xcuserdata/
*.xcuserstate

# Python tooling
tools/__pycache__/
tools/build_log.csv
